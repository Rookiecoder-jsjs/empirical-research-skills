# Synthetic workflow demo

All six observations in `data/synthetic.csv` are invented solely to exercise
the workflow runner. They are not research data and support no empirical claim.

The only step computes a descriptive mean with the Python standard library.
No scientific package or network connection is required. See the repository
README for planning, execution and resume commands. Generated reports and logs
remain under `processed/results/` and are ignored by Git.
